"use client";

import { motion } from "framer-motion";

export default function AboutMe() {
    return (
        <section className="h-1/2 bg-primary flex items-center justify-center">
          <motion.div
            className="h-52 w-52 bg-white"/>
        </section>
    );
}